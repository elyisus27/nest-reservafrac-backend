export interface PayloadInterface {
    id: number;
    username: string;
    email: string;
    profiles: string[];
}

export interface payloadProfile {
    profileId: number,
    profileName: string
}